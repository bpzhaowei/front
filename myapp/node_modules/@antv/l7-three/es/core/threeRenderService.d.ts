import { IMercator } from '@antv/l7-core';
import 'reflect-metadata';
import { Camera, WebGLRenderer } from 'three';
export interface IThreeRenderService {
    renderer: WebGLRenderer;
    camera: Camera;
    center: IMercator;
    init(): void;
    getRenderCamera(): Camera;
}
export declare const ThreeRenderServiceType: unique symbol;
export declare class ThreeRenderService implements IThreeRenderService {
    renderer: WebGLRenderer;
    camera: Camera;
    center: IMercator;
    aspect: number;
    update: () => void;
    private scene;
    private cameraTransform;
    private readonly rendererService;
    private readonly mapService;
    init(): void;
    getRenderCamera(): Camera;
    private mapboxCamera;
    private AMapCamera;
    private AMap2Camera;
}
